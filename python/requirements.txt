# Dependencies live in pyproject.toml extras — `[all]` covers numpy, pandas,
# matplotlib, jupyter, nbconvert, ipykernel, pydantic.
#
# Preferred install (after `cmake --build build --target _portopt`):
#     pip install -e ./python[all]
#
# Or use the bundled bootstrap target:
#     cmake --build build --target python-venv
#
# This file is retained so `pip install -r requirements.txt` still works for
# tools that don't read pyproject extras.
-e .[all]
